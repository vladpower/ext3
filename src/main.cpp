#include <iostream>
#include "utils/filemapping.h"
using namespace std;
int main(int argc, char* argv[]) {
    //input path to drive image file and offset from the beginning of the disk in sectors
    if(argc<2) {
        cerr<<"too few arguments"
    }

    return 0;
}
